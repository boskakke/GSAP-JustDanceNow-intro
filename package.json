{
  "name": "gsap",
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    "dev": "parcel src/intro.html",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/intro.html --no-content-hash --no-source-maps --public-url ./",
    "build": "yarn clean && npm run build:parcel"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.8.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.1",
    "parcel": "^2.0.1",
    "sharp": "^0.29.1"
  }
}
